org.gradle.parallel=true
org.gradle.caching=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Gradle phải chạy trên JDK 17 trở lên (Spring Boot 4). Mã nguồn biên dịch bằng
# JDK 21 qua toolchain — xem build.gradle.kts. Máy nào JAVA_HOME còn trỏ JDK cũ
# thì đặt org.gradle.java.home trong ~/.gradle/gradle.properties, KHÔNG đặt ở đây
# (đường dẫn của máy cá nhân không thuộc về repo).
