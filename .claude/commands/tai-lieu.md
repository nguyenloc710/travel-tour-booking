---
description: Viết một tài liệu mới trong docs/ đúng chuẩn dự án
argument-hint: "NN-ten-file  (ví dụ: 20-frontend-web)"
---

Viết tài liệu mới: **$ARGUMENTS**

Dùng skill `tai-lieu-moi`.

Trước khi viết, đọc `docs/00` để biết file này chịu trách nhiệm về nguồn sự thật nào, và đọc `docs/42` mục 6 để biết những gì **không** được nói ở đây.

Nếu nội dung phụ thuộc một quyết định đang chờ ở `docs/41` mục 4: viết phần không phụ thuộc, phần còn lại để thành mục "chưa chốt" có tên câu hỏi. Đừng tự chọn giùm rồi viết như đã chốt.

Xong thì: đăng ký vào `docs/00`, cập nhật `docs/41`, chạy `python scripts/docs_check.py`.
